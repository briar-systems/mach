[project]
name = "mach"
version = "0.0.1"
entrypoint = "main.mach"

[directories]
src-dir = "src"
out-dir = "out"

# dependencies (automatically serve as module aliases)
[deps]
std = { path = "../mach-std", src = "src" }
