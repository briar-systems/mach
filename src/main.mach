use std.runtime;

use cli: mach.cli.main;

# mach compiler entry point
# ---
# delegates to cli.run for command parsing and dispatch.

$main.symbol = "main";
fun main(argc: i64, argv: &&u8) i64 {
    ret cli.run(argc, argv);
}
