# hello world with proper print function

val msg: *u8 = "Hello, World!\n";

fun print(fd: i64, text: *u8, len: i64) {
    mir {
        syscall(1, %0, %1, %2)
    }
}

pub fun _start() {
    print(1, msg, 14);
    mir {
        syscall(60, 0)
    }
}
