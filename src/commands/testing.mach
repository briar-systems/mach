use std.types.string;

use print: std.print;

use cmd_help: mach.commands.help;

# mach.commands.testing: test command implementation.
# ---
# runs project tests.

# handle: main entry point for the test command.
# ---
# argc: argument count
# argv: argument vector
# ret:  exit code (0 = success, non-zero = error)
pub fun handle(argc: i64, argv: &&u8) i64 {
    # argv[0] = "mach", argv[1] = "test", argv[2] = path, argv[3..] = options

    val project_path: str = ".";
    if (argc >= 3) {
        val arg: str = argv[2];
        if (!arg.starts_with("-")) {
            # TODO: use argv[2] as project path
        }
    }

    # TODO: parse options (--target, --filter)
    # TODO: load mach.toml configuration
    # TODO: find test files in project
    # TODO: for each test file:
    #   - parse and extract test declarations
    #   - build AST for test harness
    #   - compile test binary
    #   - run test binary
    #   - collect and report results
    # TODO: print summary of test results

    print.eprintln("error: test command not yet implemented");
    ret 1;
}
