use std.types.string;

use print: std.print;

use cmd_help: mach.commands.help;

# mach.commands.run: run command implementation.
# ---
# runs a built Mach project executable.

# handle: main entry point for the run command.
# ---
# argc: argument count
# argv: argument vector
# ret:  exit code (0 = success, non-zero = error)
pub fun handle(argc: i64, argv: &&u8) i64 {
    # argv[0] = "mach", argv[1] = "run", argv[2..] = options/path/args

    # TODO: parse --target flag
    # TODO: find project path (default to current directory)
    # TODO: load mach.toml configuration
    # TODO: resolve target and binary path
    # TODO: execute the binary with remaining arguments

    val project_path: str = ".";
    if (argc >= 3) {
        # check if argv[2] is a flag or the project path
        val arg: str = argv[2];
        if (!arg.starts_with("-")) {
            # TODO: use this as project path
        }
    }

    print.eprintln("error: run command not yet implemented");
    ret 1;
}
