# mach.compiler.target.os.linux: Linux OS configuration

use os: mach.compiler.target.os;

# get_os: Linux OS configuration
# ---
# ret: OS record for Linux
pub fun get_os() os.OS {
    var o: os.OS;
    o.name         = "linux";
    o.entry_symbol = "_start";
    o.base_addr    = 0x400000;
    o.page_size    = 4096;
    ret o;
}
