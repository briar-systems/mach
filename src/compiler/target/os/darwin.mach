# mach.compiler.target.os.darwin: Darwin OS configuration

use os: mach.compiler.target.os;

# get_os: Darwin/macOS OS configuration
# ---
# ret: OS record for Darwin
pub fun get_os() os.OS {
    var o: os.OS;
    o.name         = "darwin";
    o.entry_symbol = "_main";
    o.base_addr    = 0x100000000;
    o.page_size    = 4096;
    ret o;
}
