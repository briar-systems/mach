use std.runtime;
use std.types.size.usize;
use std.types.string.str;

use mach.cli.cmd.dispatch;

# reached only through its symbol, by the runtime's entry
#[symbol("main")]
fun main(argc: usize, argv: *str) i64 {
    ret dispatch(argc, argv);
}
