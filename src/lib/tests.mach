# the test-only modules no compiler entry reaches: `mach test . --lib tests` runs their tests

use std.runtime;

use mach.lang.be.codegen.deep_frame_runtime;
use mach.lang.be.codegen.stack_probe_runtime;
use mach.lang.be.codegen.vector_runtime;
use ct_probe:     mach.lang.ct.probe;
use driver_tests: mach.lang.driver.tests;
use mach.lang.fe.literal_suffix_runtime;
use sema_tests: mach.lang.fe.sema.tests;
use mach.lang.fuzz;
use mach.lang.me.lower.aggregate_value_runtime;
use mach.lang.me.lower.generic_layout_runtime;
use mach.lang.target.isa.asm.extensions;
use mach.lang.target.isa.asm.host;
use mach.lang.target.isa.riscv.refcore;
use isa_tests: mach.lang.target.isa.tests;
use x64_probe: mach.lang.target.isa.x64.probe;
use mach.lang.target.of.coff_unwind_runtime;
use mach.lang.validation.catalog;
