use          std.types.string;
use console: std.io.console;

pub fun show_help() {
    console.print("usage: mach format <paths...>\n");
}

pub fun handle(args: []str) i64 {
    console.print("format command not yet implemented\n");
    ret 1;
}
