// mach runtime module
// provides the C main function that calls the user's __mach_main

// external declaration of user's main function (renamed by compiler)
extern fun __mach_main() u32;

// c main function - entry point for the program
fun main() u32 {
    ret __mach_main();
}
