.DS_Store
Thumbs.db
out

# the stages of the CONTRIBUTING.md fixpoint recipe, and the generation
# binaries other fixpoint checks leave in the root
/a
/b
/c
/a.exe
/b.exe
/c.exe
/m
/m[0-9]
/g[A-Z]

# a qemu-user leg that crashes drops its core in the working directory, so an
# untracked-file sweep picks them up and commits them
*.core

# transaction control files a pre-#3310 formatter left beside the sources

# python bytecode a local run of a workflow helper leaves beside it
__pycache__/
