error: import 'provider_valu' is pinned to library 'provider-lib', which this link provides as a static input. a static input defines symbols rather than importing them, so nothing binds the import: the symbol is not defined by any object or archive in the link. check the declaration's spelling against the library's own symbols, or that the archive was built with it
