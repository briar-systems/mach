# one of two sibling modules that each take the ADDRESS of the same cross-module
# `#[inline]` function. two modules are the whole point: one could not disagree with
# itself.
use c: case.counter;

pub fun addr() u64 { ret (c.adouble)::u64; }
