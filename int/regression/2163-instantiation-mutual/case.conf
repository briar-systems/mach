# the compile must be REFUSED; the diagnostic is the observable. one leg is
# enough: the refusal is a middle-end policy, decided before any target-specific
# lowering, so every target reports the same text.
run: build-fails
targets: linux
