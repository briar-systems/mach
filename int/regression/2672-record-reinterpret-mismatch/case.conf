# #2672 made the size check MEASURE a record instead of reading it as 0 bytes. the
# other half of that is that a genuine size mismatch must still be rejected, and
# rejected with the real sizes named. a fix that simply stopped checking would pass
# every accept-side case and only this one would notice.
run: build-fails
targets: linux
profiles: debug
