error: dynamic import '__imp_Sleep' names the import-address alias of 'Sleep' rather than an export: '__imp_' is the convention for the address cell the import table itself synthesizes, so no library exports a symbol under that name and the image would fail to load. import 'Sleep' instead - the linker emits that import and resolves every alias reference to its address cell
