# a `[step]` must actually spawn its shell on a native windows host. mach runs
# steps under `cmd.exe /c` there, and `exec.run` performs no PATH search, so a
# bare shell name never launches (#2578). the step's output is embedded at
# compile time, so a step that does not run cannot produce a passing binary.
run: exec
targets: windows
