# 128-bit vector work through the spirv back half, validated host-side by
# spirv-val. nothing executes: a SPIR-V module is a GPU artifact, so the leg needs
# the Khronos validator, not a runner.
run: spirv-val
targets: linux
build-target: spirv
