[project]
id = "case"
name = "case"
description = "integration test fixture"
version = "0.0.0"
src = "src"
out = "out/{target.name}/{profile.name}"

[target.freestanding-aarch64]
isa = "aarch64"
os  = "freestanding"
abi = "aapcs64"

[profile.debug]
opt = 0

[profile.release]
opt = 2

[artifact.case]
kind = "bin"
entry = "main.mach"
out = "bin/case"
targets = ["*"]
