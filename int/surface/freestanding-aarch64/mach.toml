[project]
id      = "case"
name    = "case"
version = "0.0.0"
src     = "src"
target  = "freestanding-aarch64"
out     = "out/{target}/{profile}/bin/{name}{ext}"

[target.freestanding-aarch64]
isa = "aarch64"
os  = "freestanding"
abi = "aapcs64"

[profile.debug]
opt = 0

[profile.release]
opt = 2

[bin.case]
entry = "main.mach"
