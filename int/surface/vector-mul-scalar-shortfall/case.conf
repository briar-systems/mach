# only the three legs whose realization of a 64-bit lane multiply this pins. the
# darwin legs run the same two ISAs and would restate it.
targets: linux linux-arm64 linux-riscv64
profiles: require
run: build-fails
