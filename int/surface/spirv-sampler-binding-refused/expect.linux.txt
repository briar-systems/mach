error: an image handle cannot be a record or union field: a handle names a descriptor rather than an object with storage, and a SPIR-V struct may not contain one. bind the handle itself with `#[sampler(set, binding)]`
error: an image handle cannot be a record or union field: a handle names a descriptor rather than an object with storage, and a SPIR-V struct may not contain one. bind the handle itself with `#[sampler(set, binding)]`
error: an image handle binding needs a `#[sampler(set, binding)]` decorator: a shader has no general global storage, so a handle with no descriptor address is reachable from no stage
error: `sampler` decorator applies only to an image handle binding (`sampler2d`, `texture2d`, `sampler`, and their dimensional relatives); a descriptor-bound block or buffer of an ordinary type is `#[uniform(set, binding)]` or `#[storage(set, binding)]`
error: an image handle is bound as a descriptor and carries `#[sampler(set, binding)]`; the role on it puts the handle in a storage class no image, sampler, or sampled-image variable may live in
error: an image handle cannot sit behind a pointer or inside an array here: a handle names a descriptor rather than an object with an address, and the logical addressing a shader uses has no pointer to one. declare the handle itself
error: an image handle cannot sit behind a pointer or inside an array here: a handle names a descriptor rather than an object with an address, and the logical addressing a shader uses has no pointer to one. declare the handle itself
error: an image handle cannot be a local binding: SPIR-V forbids storing an image, sampler, or sampled-image object, so the local's storage has nothing that may be written into it. hand the handle straight to the operation that consumes it instead of naming it first
