error: a handle cannot be a record or union field: a record is storage the program owns at offsets it can compute, and a handle's representation is not the program's, so an aggregate holding one has no layout. declare the handle itself
error: a handle cannot be a record or union field: a record is storage the program owns at offsets it can compute, and a handle's representation is not the program's, so an aggregate holding one has no layout. declare the handle itself
error: a handle binding needs a `#[sampler(set, binding)]` decorator: a shader has no general global storage, so a handle with no descriptor address is reachable from no stage
error: `sampler` decorator applies only to a handle binding, a `def` declared with `#[handle(...)]`; a descriptor-bound block or buffer of an ordinary type is `#[uniform(set, binding)]` or `#[storage(set, binding)]`
error: a handle is bound as a descriptor and carries `#[sampler(set, binding)]`; the role on it puts the handle in a storage class no handle may live in
error: a handle cannot sit behind a pointer or inside an array: a pointer and an array are both extents the program computes, and a handle's extent is the owning target's rather than the program's, so there is no element size to step by. declare the handle itself
error: a handle cannot sit behind a pointer or inside an array: a pointer and an array are both extents the program computes, and a handle's extent is the owning target's rather than the program's, so there is no element size to step by. declare the handle itself
error: a handle cannot be a local binding: a local is storage the program owns, and a handle's representation is not the program's, so there is nothing that may be written into it. hand the handle straight to the operation that consumes it instead of naming it first
