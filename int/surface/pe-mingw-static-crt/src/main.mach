# The C caller uses vsnprintf. Its implementation, an earlier options helper,
# and the UCRT short-import record are selected transitively from one archive;
# the archive's unrelated MinGW startup member must remain unselected.
ext fun call_format() i32;

#[symbol("mainCRTStartup")]
pub fun _start() {
    call_format();
}
