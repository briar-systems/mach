error: `sampler3d` is spelled as an image handle type, so it cannot also name a type; the declaration would be unreachable
error: `sampler2dshadow` is spelled as an image handle, and depth-comparison sampling is out of scope for the first sampled-image cut (#2794): a `shadow` handle is sampled by `OpImageSampleDref*`, which takes a reference value the sampling operation here has no operand for. it is refused rather than emitted as an ordinary image, which would compile and compare nothing
error: `sampler2dms` is spelled as an image handle, and a multisampled image is not sampled at all: it is FETCHED per sample by `OpImageFetch` with a `Sample` image operand, which is a different instruction with an operand mask this cut does not carry. an `ms` handle sampled as an ordinary one would be an invalid module
error: `samplerrect` is spelled as an image handle, and this dimensionality needs a capability and a sampling path beyond the ones emitted here: `rect` and `buffer` are unnormalized-coordinate and texel-buffer images, and `subpass` is read by `OpImageRead` within a render pass. `1d`, `2d`, `3d` and `cube` are the dimensionalities this cut binds
error: `samplerbuffer` is spelled as an image handle, and this dimensionality needs a capability and a sampling path beyond the ones emitted here: `rect` and `buffer` are unnormalized-coordinate and texel-buffer images, and `subpass` is read by `OpImageRead` within a render pass. `1d`, `2d`, `3d` and `cube` are the dimensionalities this cut binds
error: `image2d` is spelled as an image handle, and a storage image (`image...`) is read and written directly rather than sampled, so it carries an Image Format operand this cut does not spell and needs `OpImageRead` / `OpImageWrite`. a `sampler...` or `texture...` handle is the sampled form
