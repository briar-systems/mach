# aarch64 only, and that is the point: this leg is where the packed form exists. the
# same source under `require` on x86-64 or riscv64 is a build error, which is what
# vector-mul-scalar-shortfall's siblings pin.
targets: linux-arm64 darwin-aarch64
profiles: require
