error: cannot take the address of a field of a `#[packed]` record: the field sits at an offset its own type does not guarantee, so the resulting pointer would claim an alignment the storage does not have. copy the field into a local and take that address, or take the address of the whole record (`?r`), whose pointer type is honest about being align 1
