# Cross-link real Clang COFF aliases, a selected short-import archive, a discarded
# SELECT_ANY user, and later local providers. Only the live alias gets a cell; the
# stale declaration and dead COMDAT produce no loader or base-relocation residue.
run: pe-local-import
targets: linux
build-target: windows
