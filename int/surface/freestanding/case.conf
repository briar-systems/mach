# build the os=freestanding, of=raw flat image on the linux leg and run it through
# the C mmap-loader; the observable is the image's exit status (its computed sum).
run: flat-loader
targets: linux
build-target: freestanding-x86_64
