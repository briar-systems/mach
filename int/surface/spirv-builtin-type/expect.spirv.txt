error: this `#[builtin("global_invocation")]` variable is declared `i32x4` / `u32x4`, and SPIR-V specifies it as `u32x3`, a three-component 32-bit unsigned vector. the requirement is on the variable's TYPE rather than on how it is read, so a wider or narrower one is an invalid module rather than a wasteful one
