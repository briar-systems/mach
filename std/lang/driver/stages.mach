use std.types.string;

pub def PipelineStage:  u8;
pub val STAGE_NONE:     PipelineStage = 0;
pub val STAGE_LEXICAL:  PipelineStage = 1;
pub val STAGE_SYNTAX:   PipelineStage = 2;
pub val STAGE_SEMANTIC: PipelineStage = 3;
pub val STAGE_LOWERING: PipelineStage = 4;
pub val STAGE_CODEGEN:  PipelineStage = 5;
pub val STAGE_COMPLETE: PipelineStage = 6;

pub fun (this: PipelineStage) name() str {
    if (this == STAGE_NONE)     { ret "initialization"; }
    if (this == STAGE_LEXICAL)  { ret "lexical"; }
    if (this == STAGE_SYNTAX)   { ret "syntax"; }
    if (this == STAGE_SEMANTIC) { ret "semantic analysis"; }
    if (this == STAGE_LOWERING) { ret "lowering"; }
    if (this == STAGE_CODEGEN)  { ret "code generation"; }
    if (this == STAGE_COMPLETE) { ret "complete"; }
    ret "unknown";
}
