# the tree is canonical byte for byte on every host: a windows checkout must not
# turn LF into CRLF, or `mach fmt --check` there reads a different file from the
# one that was formatted
* text=auto eol=lf
# fuzz inputs are bytes, some of them deliberately CRLF or invalid text
test/fuzz/corpus/** -text
