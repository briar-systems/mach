use std.runtime;
use std.print;
use std.types.size.usize;

ext fun got_probe() i32;

#[symbol("main")]
fun main(argc: usize, argv: **u8) i64 {
    val result: i32 = got_probe();
    print.printlnf("imported={}", result);
    if (result != 1) { ret 1; }
    ret 0;
}
