#[volatile]
rec Registers { x: u32; y: u32; }
#[storage(0, 0)] var source: Registers;
#[storage(0, 1)] var destination: Registers;
#[stage("fragment")]
fun frag_main() {
    destination = source;
    destination.x = source.x;
}
