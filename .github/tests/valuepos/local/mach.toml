[project]
id = "local"
name = "value-position diagnostics — local type names"
version = "0.0.0"
src = "src"
dep = "dep"
target = "linux"
out = "out/{target}/bin/{name}{ext}"
obj = "out/{target}/obj"

[target.linux]
isa = "x86_64"
os  = "linux"
abi = "sysv64"

[bin.local]
entry = "main.mach"
