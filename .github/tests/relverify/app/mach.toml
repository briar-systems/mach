[project]
id = "relverify"
name = "release --verify-ir regression — App"
version = "0.0.0"
dir_src = "src"
dir_out = "out"
dir_dep = "dep"
target = "linux"

[targets.linux]
os = "linux"
isa = "x86_64"
abi = "sysv64"
mode = "library"
entrypoint = "main.mach"
artifacts = "linux"
binary = "linux/lib/relverify.o"
