name: CI

on:
  pull_request:
    branches: [dev]

jobs:
  build:
    name: Full Build (4-stage)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Build mach
        run: make
