use mach.lang.layout;

rec Bytes {
}

rec Words {
}

fun reject_cross_unit_alignment() {
    var position: layout.CheckedOffset[Bytes];
    var align: layout.CheckedAlignment[Words];
    layout.align_offset_up[Bytes](position, align);
}
