# probes a reviewed statement declares away, one per line:
#
#   <target>  <probe symbol>  <reason>
#
# an entry is a declared gap between the catalog and the realization, never a way
# to make a red cell green: the driver reports the cell as an exception, counts it
# apart from passes, and fails the run if the entry matches nothing, so a fix that
# lands deletes its line.
#
