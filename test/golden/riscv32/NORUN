# cases the riscv32 column builds and diffs but does not run: a case name per line,
# then the reason. the golden still carries the column; only the differential under
# qemu-riscv32 is skipped, because the compiler's answer is known to be wrong today.
# entries are written out by name so a case added later inherits nothing.
#
# every entry is a MACH DEFECT, names its issue, and is deleted by the change
# that closes it. the run still executes each one and fails when it agrees with
# the reference, so a line cannot outlive its defect. the reasons name the two
# probes that isolate the families this file was written against; a case is
# attributed to a family only where its source carries the pattern the probe
# isolates, otherwise it is unattributed and the fix for #3616 re-runs it.
#
#   U64 INDEX (#3615). `var k: u64 = seed + 3; av[k]` reads av[0]: the index is
#   taken from a register no instruction assigned (`slli t0, a2, 2` with k in
#   a4:a1). a u32 index is correct. every case below that indexes an array with
#   a u64 fails.
#
#   SUB-WORD WIDEN (#3616). `var a: u8 = 240 + s; a = a + 8; a::u64` yields
#   0x108: the widening emits no mask, so wrapped u8/u16 arithmetic leaks its
#   carry bits.
#
# 46 of the 108 cases that build pass the differential at o0 and o2.

# ---- U64 INDEX (#3615) ----

call/call_chain            o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
call/call_indirect         o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
call/call_int_regs         o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
call/call_mixed            o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
call/call_rec_edge         SIGSEGV at o0 under qemu-riscv32; indexes an array with a u64 (#3615).
call/call_rec_large        o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
call/call_rec_mutate       o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
call/call_rec_small        o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
call/call_recursive        SIGSEGV at o0 under qemu-riscv32; indexes an array with a u64 (#3615).
call/call_ret_large        o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
call/call_ret_small        o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
call/call_variadic         o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
cmp/cmp_i32                o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
cmp/cmp_i64                o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
cmp/cmp_u32                o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
cmp/cmp_u64                o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
cmp/short_circuit          SIGSEGV at o0 under qemu-riscv32; indexes an array with a u64 (#3615).
comptime/ct_runtime_agree  o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
float/cmp_f64              o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
frame/frame_align          SIGSEGV at o0 under qemu-riscv32; indexes an array with a u64 (#3615).
frame/frame_large          o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
imm/imm_addr               o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
imm/imm_mov                o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
mem/addr_modes             o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
mem/array_index            o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
mem/escaping_element       o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
mem/global_data            o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
mem/global_zero            o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
mem/loadstore              o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
mem/ptr_arith              o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
mem/uni_layout             o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
tag/array                  SIGSEGV at o0 under qemu-riscv32; indexes an array with a u64 (#3615).
tag/call_ret               SIGSEGV at o0 under qemu-riscv32; indexes an array with a u64 (#3615).
tag/guards                 SIGSEGV at o0 under qemu-riscv32; indexes an array with a u64 (#3615).
vec/autovec_loop           o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
vec/vec_cmp_i64            o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
vec/vec_f32x2              o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
vec/vec_f32x3              o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
vec/vec_f32x4              o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
vec/vec_f32x5              o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
vec/vec_f32x8              o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
vec/vec_f64x2              o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
vec/vec_i16x4              o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
vec/vec_i16x8              o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
vec/vec_i32x4              o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
vec/vec_i64x2              o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
vec/vec_i8x16              o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
vec/vec_mem                o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
vec/vec_u16x8              o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
vec/vec_u32x4              o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
vec/vec_u64x2              o0 checksum differs from the C reference; indexes an array with a u64 (#3615).
vec/vec_u8x16              o0 checksum differs from the C reference; indexes an array with a u64 (#3615).

# ---- SUB-WORD WIDEN (#3616) ----

scalar/arith_u8            o0 checksum differs from the C reference; u8 chains widened through mix_u8 (#3616).
scalar/arith_i8            o0 checksum differs from the C reference; i8 chains widened through mix_i8 (#3616).
scalar/arith_u16           o0 checksum differs from the C reference; u16 chains widened through mix_u16 (#3616).
scalar/arith_i16           o0 checksum differs from the C reference; i16 chains widened through mix_i16 (#3616).
vec/rows_i8                o0 checksum differs from the C reference; 8-bit lanes scalarized and widened (#3616).
vec/rows_i16               o0 checksum differs from the C reference; 16-bit lanes scalarized and widened (#3616).

# ---- UNATTRIBUTED (#3616) ----

convert/ext_sign           o0 checksum differs from the C reference; unattributed, re-run by the fix for #3616.
convert/trunc              o0 checksum differs from the C reference; unattributed, re-run by the fix for #3616.
vec/mul_wide               o0 checksum differs from the C reference; unattributed, re-run by the fix for #3616.
vec/vec_lane_ops           o0 checksum differs from the C reference; unattributed, re-run by the fix for #3616.
