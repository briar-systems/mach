# the cases this column serves: a case name or glob per line, then the reason.
# riscv64-linux with Zkt selected changes codegen only by admitting the secret
# multiply, so the column serves the ct group and nothing else: every other case
# would repeat the riscv64-linux golden.

ct/*  the secret multiply cases; riscv64-linux carries every other group.
