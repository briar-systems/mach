# cases this column cannot serve: a case name per line, then the reason.

ct/mul_secret  a secret multiply is refused: the instruction set's rows hold only under DIT/DOITM, which no OS declares yet (#3508).
ct/mul_secret128  same refusal: the secret widening multiply of two 64-bit limbs is a DIT row (#3508).
