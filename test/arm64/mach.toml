[project]
id = "a64probe"
name = "a64probe"
version = "0.0.0"
src = "src"
target = "linux-arm64"
out = "out/{target}/{name}{ext}"
obj = "out/{target}/obj"

[target.linux-arm64]
isa = "aarch64"
os  = "linux"
abi = "aapcs64"

[profile.debug]
opt = 0

[bin.a64probe]
entry = "main.mach"
