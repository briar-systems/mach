# verifies an exported foreign object generated by a path-dependency step

use std.print;
use std.runtime;
use std.types.size.usize;

ext fun provider_value() i32;

#[symbol("main")]
fun main(argc: usize, argv: **u8) i64 {
    print.printlnf("provider={}", provider_value());
    ret 0;
}
