# a third module embedding the same content again, so the fixture observes a
# coalesce chain rather than a single pair

#[embed("asset_dup.bin")]
pub val LIBB_ASSET: [_]u8;

pub fun libb_sum() i64 { ret (LIBB_ASSET[16])::i64; }
