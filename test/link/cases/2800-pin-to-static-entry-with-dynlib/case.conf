# the exact shape #2800 was reported as, and the arm the old message got wrong in
# the most misleading way: the pinned library is a STATIC entry, and the link also
# has an unrelated DYNAMIC dependency. the old compiler answered "pinned to library
# 'provider-lib' not among the link's dependencies" - a false statement about a
# library the manifest does provide - which sent the report at the export cascade
# instead of at the misspelled entry point that was actually wrong.
#
# ITS GOLDEN IS BYTE-IDENTICAL TO `2800-pin-to-static-entry`, WHICH IS THE ASSERTION.
# that case is the same program with the dynamic entry removed. the two goldens
# matching is what pins that the wording is decided by the attribution and the link
# set, and not, as it once was, by whether some unrelated shared library happened to
# be in the link. if a future change makes these two diverge again, both cases fail.
run: build-fails
legs: x86_64-linux
profiles: debug
