# the win64 arm of 3461-byref-param-location: a 32-byte record is passed as the
# address of a caller copy, and the DWARF sits in COFF sections. cross-built on the
# x86_64-linux leg and read host-side, never executed.
run: byref-param-location
legs: x86_64-linux
target: x86_64-windows
gbuild: yes
