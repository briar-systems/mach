# parse the cross-built win64 PE image and every COFF object it links from with
# llvm-readobj, host-side, and name the image's sections (#3113).
run: readobj
legs: x86_64-linux
target: x86_64-windows
