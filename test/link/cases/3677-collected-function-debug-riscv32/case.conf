# a riscv32 -g bin with a live _start and a function nothing reaches: the
# collected function's .debug_info code address takes the 32-bit tombstone and
# the link succeeds, where the 64-bit tombstone overflowed the abs32 field
run: built
legs: x86_64-linux
target: riscv32
profiles: debug
