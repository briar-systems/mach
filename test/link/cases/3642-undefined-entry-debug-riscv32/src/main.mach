# no _start anywhere: the only function is unreachable from an entry the image
# does not have, and its .debug_info code address must not be what the link
# complains about

pub fun keep(seed: u64) u64 {
    var a: u64 = seed * 7;
    ret a + 1;
}
