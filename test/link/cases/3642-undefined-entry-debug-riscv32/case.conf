# a riscv32 -g bin with no _start: the link reports the undefined entry, the
# diagnostic the plain build gives, not a relocation the debug sections carry
# against the functions that missing entry leaves dead
run: build-fails
legs: x86_64-linux
target: riscv32
profiles: debug
