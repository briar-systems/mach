# the ELFCLASS32 twin of readobj-elf-x86_64: a freestanding riscv32 image, cross-built
# on the x86_64-linux leg and decoded host-side with llvm-readobj, so the RV32 static
# and relocatable writer paths are read independently (#3113).
run: readobj
legs: x86_64-linux
target: riscv32
