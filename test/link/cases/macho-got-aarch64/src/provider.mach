# the cells main.mach imports, defined under the C spelling the import resolves
# to on darwin. the module is a separate object, so the reads are cross-object
#[symbol("_cell8")]
pub var cell8: u8 = 7;
#[symbol("_cell64")]
pub var cell64: u64 = 35;
