# build the os=freestanding, of=raw flat image on the x86_64-linux leg and run it through
# the C mmap-loader; the observable is the image's exit status (its computed sum).
run: flat-loader
legs: x86_64-linux
target: freestanding-x86_64
