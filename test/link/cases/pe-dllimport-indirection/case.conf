# Cross-link a real Clang COFF dllimport reference on Linux, then inspect the PE
# import table and the foreign instruction's patched rel32 target. The Mach module
# also calls the same export directly, proving X and __imp_X share one IAT entry.
run: pe-dllimport
legs: x86_64-linux
target: x86_64-windows
