# Cross-link the C/MinGW-shaped archive on Linux and inspect the PE import table.
# The only import must be the UCRT helper used by the selected static vsnprintf;
# pulling the unrelated startup member would add __imp_GetStartupInfoA and fail.
run: pe-imports
legs: x86_64-linux
target: x86_64-windows
