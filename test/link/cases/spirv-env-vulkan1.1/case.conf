# a vulkan1.1 module is SPIR-V 1.3: its storage buffers are StorageBuffer variables
# over Block blocks, and its entry points still list only Input and Output variables
# (#3399). the source is spirv-env-vulkan1.0's, so the two goldens differ only where
# the version does.
run: spirv-val-pinned
legs: x86_64-linux
target: spirv
