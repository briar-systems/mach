error: dynamic import 'some_unattributed_import' has no #[library] attribution; a two-level-namespace target requires every import to name its providing library - annotate the declaration with #[library("...")], or, when the symbol arrives from a static archive member and has no declaration to annotate, claim it with symbols = ["some_unattributed_import"] on the [link.<entry>] naming its library; candidate libraries: kernel32 (kernel32.dll)
