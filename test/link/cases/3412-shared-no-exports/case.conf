# the other side of #3412: a `kind = "shared"` artifact whose project declares no
# `pub` and re-exports nothing has no export surface, so nobody can call into it
# and link GC has no root to keep anything alive. the link refuses it by name
# before it plans an image, because the diagnostic that reaches the author has to
# name the rule rather than the empty image that would otherwise follow.
run: build-fails
legs: x86_64-linux
profiles: debug
