# the riscv64 ELF twin of readobj-elf-x86_64: cross-built on the x86_64-linux leg
# and read host-side, so the riscv64 writer path is decoded independently without
# a riscv64 runner (#3113).
run: readobj
legs: x86_64-linux
target: riscv64-linux
