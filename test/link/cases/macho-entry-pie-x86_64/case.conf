# Execute a `--pie` (MH_PIE / LC_MAIN) darwin x86_64 image on the native Intel
# macOS leg and assert the argument block dyld handed the entry. Native-only: the
# contract under test is what dyld passes in registers, which no host-side
# structural inspection can observe.
run: exec
legs: x86_64-darwin
build-flags: --pie
