# Cross-link a Windows image on Linux and inspect the relocated CodeView records.
# The checked-in input is a real clang COFF object whose over-aligned `.text`
# makes its final PE section base differ from the linker's content base.
run: pe-codeview
legs: x86_64-linux
target: x86_64-windows
