[project]
id = "shapes"
version = "0.0.0"
src = "src"
out = "out/{target.name}/{profile.name}"

[artifact.shapes]
default = true
kind = "static"
entry = "lib.mach"
out = "lib/shapes"
targets = ["*"]
link = []
need = []
