# a type declared in a dependency, with its identity taken inside the dependency
pub rec Point { x: u64; }
pub rec Box[T] { v: T; }

pub fun id_of[T]() u64 { ret $type_id(T); }
pub fun point_id() u64 { ret $type_id(Point); }
pub fun box_id() u64 { ret id_of[Box[u64]](); }
