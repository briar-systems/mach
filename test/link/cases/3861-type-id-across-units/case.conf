# $type_id of one type is equal in every object of one program and distinct between types
# (#3861). a dependency and two of the root's modules each derive identities in their own
# object, and main compares them at run time, so a value that depended on which object
# computed it, or on how the linker coalesced the weak generic instances, would differ.
legs: x86_64-linux
