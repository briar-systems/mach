# a second module instantiating the same generic over the same types
use shapes;

pub fun dep_point_id() u64 { ret $type_id(shapes.Point); }
pub fun box_id() u64 { ret shapes.id_of[shapes.Box[u64]](); }
pub fun box32_id() u64 { ret shapes.id_of[shapes.Box[u32]](); }
