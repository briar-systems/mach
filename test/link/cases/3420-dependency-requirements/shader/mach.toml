[project]
id = "shader"
version = "0.0.0"
src = "src"
out = "out/{target.name}/{profile.name}"

[target.x86_64-linux]
isa = "x86_64"
os  = "linux"
abi = "sysv64"

[target.spirv]
isa = "spirv"
os  = "freestanding"
abi = "spirv"

[artifact.shader]
default = true
kind = "static"
entry = "lib.mach"
out = "lib/shader{artifact.suffix}"
targets = ["x86_64-linux"]
link = []
need = ["artifact.shader-quad"]

[artifact.shader-quad]
kind = "bin"
entry = "quad.mach"
out = "spv/quad{artifact.suffix}"
targets = ["spirv"]
link = []
need = []

[dep.std]
path = "../../../../../dep/std"
