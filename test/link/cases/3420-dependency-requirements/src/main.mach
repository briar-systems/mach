use std.runtime;
use p: std.print;
use boom;
use shader;

#[symbol("main")]
fun main() i64 {
    p.printlnf("boom len={} sum={}", boom.quad_len(), boom.quad_sum());
    p.printlnf("shader len={} sum={}", shader.quad_len(), shader.quad_sum());
    ret 0;
}
