[project]
id = "case"
version = "0.0.0"
src = "src"
out = "out/{target.name}/{profile.name}"

[target.freestanding-x86_64]
isa = "x86_64"
os  = "freestanding"
abi = "sysv64"

[profile.debug]
default = true
vectorize = true
float_reassoc = false
opt = 0
debug = false
simd = "scalarize"

[profile.release]
vectorize = true
float_reassoc = false
opt = 2
debug = false
simd = "scalarize"

[artifact.case]
kind = "bin"
entry = "main.mach"
out = "bin/case"
targets = ["*"]
link = []
need = []
