# parse the cross-built x86_64 Mach-O image and every Mach-O object it links from
# with llvm-readobj, host-side, and name the image's sections and segments (#3113).
run: readobj
legs: x86_64-linux
target: x86_64-darwin
