use helperdep.api;

# the middle link of the chain: the root module re-exports this name, and the
# definition is two modules away in a dependency
fwd api.chained_impl;

pub fun parts_add(n: i64) i64 { ret n + parts_private(n); }

fun parts_private(n: i64) i64 { ret n * 3; }
