# the project's entry. it imports `sys` so the test build loads it and collects
# its test; the load walk reaches `sys` through this `use`.

use case.sys;
use std.runtime;
use std.types.size.usize;

#[symbol("main")]
fun main(argc: usize, argv: **u8) i64 {
    ret 0;
}
