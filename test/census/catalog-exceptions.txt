# closed catalogs the catalog-defaults census skips, one type name per line.
# the policy (#3124): no production site answers an unknown member of a closed
# catalog with a default; a malformed input member, a declared member a target
# does not honor, and a member the compiler itself produced are reported as
# three distinct failure classes through mach.lang.fail.Catalog. every catalog
# is covered today
