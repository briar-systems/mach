use std.runtime;
use std.types.string;
use std.types.size;

$main.symbol = "main";
fun main(argc: i64, argv: &&u8) i64 {
    val a: str = "abc";
    val b: str = "abc";
    var i: usize = 0;
    if (a[i] != b[i]) { ret 1; }
    ret 0;
}
