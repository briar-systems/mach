use std.runtime;

$main.symbol = "main";
fun main(argc: i64, argv: &&u8) i64 {
    val y: i32 = -1;
    if (y != -1i32) { ret 1; }
    var z: i32 = 0;
    z = z - 1;
    if (y != z) { ret 2; }
    ret 0;
}
