# Field indexing and method receiver behavior

rec Vec {
    data: [4]u8;
}

fun (this: Vec) get(i: usize) u8 {
    ret this.data[i];
}

fun main() {
    val v: Vec = Vec{ data: [4]u8{ 11, 12, 13, 14 } };
    if (v.get(2) == 13u8) {
        ret 0;
    }
    ret 1;
}
