use std.types.string;

# Verify std string equals method behaves on literals

fun main() {
    val a: str = "hello";
    val b: str = "hello";
    val c: str = "world";

    if (a.equals(b) && !a.equals(c)) {
        ret 0;
    }

    ret 1;
}
