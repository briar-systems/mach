use std.runtime;
use lib: regression.lib_04_abi_return;

$main.symbol = "main";
fun main(argc: i64, argv: &&u8) i64 {
    val p: lib.Pair = lib.make_pair();
    val pp: &lib.Pair = ?p;
    if (pp.a != 11i32) { ret 1; }
    if (pp.b != 22i32) { ret 2; }
    ret 0;
}
