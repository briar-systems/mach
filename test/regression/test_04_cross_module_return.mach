use helper: regress.lib_04_abi_return;

# Ensure cross-module return-by-value of a small record is correct

fun main() {
    val p: helper.Point = helper.make();
    if (p.x == 13 && p.y == 37) {
        ret 0;
    }
    ret 1;
}
