use std.runtime;

pub val NEG_LIT:i32 = -1;
pub val NEG_EXPR:i32 = 0 - 1;

$main.symbol = "main";
fun main(argc: i64, argv: &&u8) i64 {
    var x:i32 = 0;
    x = x - 1;
    if (NEG_LIT != x) { ret 1; }
    if (NEG_EXPR != x) { ret 2; }
    if (NEG_EXPR + 2i32 != 1i32) { ret 3; }
    ret 0;
}
