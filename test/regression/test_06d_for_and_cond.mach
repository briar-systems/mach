use std.runtime;
use std.types.size;

$main.symbol = "main";
fun main(argc: i64, argv: &&u8) i64 {
    var i: usize = 0;
    var j: usize = 0;
    for (i < 3 && j < 3) {
        i = i + 1;
        j = j + 1;
    }
    if (i != 3) { ret 1; }
    if (j != 3) { ret 2; }
    ret 0;
}
