use std.runtime;
use std.types.size;

$main.symbol = "main";
fun main(argc: i64, argv: &&u8) i64 {
    var i:usize = 0;
    for (i < 10) {
        i = i + 1;
    }

    if (i != 10) { ret 1; }
    ret 0;
}
