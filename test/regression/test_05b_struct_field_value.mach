use std.runtime;

rec Pair {
    a: i32;
    b: i32;
}

$main.symbol = "main";
fun main(argc: i64, argv: &&u8) i64 {
    val p: Pair = Pair { a: 11i32, b: 22i32 };
    if (p.a != 11i32) { ret 1; }
    if (p.b != 22i32) { ret 2; }
    ret 0;
}
