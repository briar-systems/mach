use std.runtime;
use std.types.string;

fun make_str() str {
    ret "ok";
}

$main.symbol = "main";
fun main(argc: i64, argv: &&u8) i64 {
    val s: str = make_str();
    if (s[0] != 'o') { ret 1; }
    if (s[1] != 'k') { ret 2; }
    if (s[2] != 0) { ret 3; }
    ret 0;
}
