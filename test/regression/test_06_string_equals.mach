use std.runtime;
use std.types.string;
use std.types.bool;

$main.symbol = "main";
fun main(argc: i64, argv: &&u8) i64 {
    if (!"abc".equals("abc")) { ret 1; }
    if ("abc".equals("abd")) { ret 2; }
    if (!"".equals("")) { ret 3; }
    ret 0;
}
