# mach.lang.target.isa.x64

## val HAS_FLOAT

```mach
pub val HAS_FLOAT: bool = true
```

## val EXT_SSSE3

```mach
pub val EXT_SSSE3:    u64 = 0x1
```

the x86_64 extension vocabulary over the SSE2 baseline. a row is a name the
manifest, `$mach.build.extensions.<name>`, `#[extensions(...)]` and the
inline-asm `requires:` rows all spell; a row nothing encodes against yet is
still a declared, probed requirement (#3741)

## val EXT_SSE41

```mach
pub val EXT_SSE41:    u64 = 0x2
```

## val EXT_SHA

```mach
pub val EXT_SHA:      u64 = 0x4
```

## val EXT_FSGSBASE

```mach
pub val EXT_FSGSBASE: u64 = 0x8
```

## val EXT_POPCNT

```mach
pub val EXT_POPCNT:   u64 = 0x10
```

## val EXT_LZCNT

```mach
pub val EXT_LZCNT:    u64 = 0x20
```

## val EXT_BMI1

```mach
pub val EXT_BMI1:     u64 = 0x40
```

## val EXT_SSE42

```mach
pub val EXT_SSE42:    u64 = 0x80
```

## val EXT_CX16

```mach
pub val EXT_CX16:     u64 = 0x100
```

## val EXT_AVX

```mach
pub val EXT_AVX:      u64 = 0x200
```

## val EXT_AVX2

```mach
pub val EXT_AVX2:     u64 = 0x400
```

## val EXT_BMI2

```mach
pub val EXT_BMI2:     u64 = 0x800
```

## val EXT_FMA

```mach
pub val EXT_FMA:      u64 = 0x1000
```

## val EXT_MOVBE

```mach
pub val EXT_MOVBE:    u64 = 0x2000
```

## val EXT_F16C

```mach
pub val EXT_F16C:     u64 = 0x4000
```

## val EXT_AVX512F

```mach
pub val EXT_AVX512F:  u64 = 0x8000
```

## val EXT_AVX512BW

```mach
pub val EXT_AVX512BW: u64 = 0x10000
```

## val EXT_AVX512CD

```mach
pub val EXT_AVX512CD: u64 = 0x20000
```

## val EXT_AVX512DQ

```mach
pub val EXT_AVX512DQ: u64 = 0x40000
```

## val EXT_AVX512VL

```mach
pub val EXT_AVX512VL: u64 = 0x80000
```

## val EXTENSION_COUNT

```mach
pub val EXTENSION_COUNT: u32 = 20
```

## val EXTENSIONS

```mach
pub val EXTENSIONS: [EXTENSION_COUNT]extension.Extension = [EXTENSION_COUNT]extension.Extension;
```

sse41 brings ssse3 (sse3 is not modelled, so the chain stops there), sse42
brings sse41, avx brings sse42, avx2, fma and f16c bring avx, and every
avx512 set brings avx512f, which brings avx2

## val LEVEL_V2

```mach
pub val LEVEL_V2: u64 = EXT_SSSE3 | EXT_SSE41 | EXT_SSE42 | EXT_POPCNT | EXT_CX16
```

the published x86-64 microarchitecture levels as manifest spellings. each
level lists its own members over the v1 baseline (sse2, cmpxchg8b and
lahf-sahf are the baseline and are not rows) and includes every lower level;
closing the selection over `implies` adds nothing a level does not already
name. doc/language/manifest.md carries the same table and a test holds the
two together (#3741)

## val LEVEL_V3

```mach
pub val LEVEL_V3: u64 = LEVEL_V2 | EXT_AVX | EXT_AVX2 | EXT_BMI1 | EXT_BMI2 | EXT_FMA | EXT_LZCNT | EXT_MOVBE | EXT_F16C
```

## val LEVEL_V4

```mach
pub val LEVEL_V4: u64 = LEVEL_V3 | EXT_AVX512F | EXT_AVX512BW | EXT_AVX512CD | EXT_AVX512DQ | EXT_AVX512VL
```

## val LEVEL_COUNT

```mach
pub val LEVEL_COUNT: u32 = 3
```

## val LEVELS

```mach
pub val LEVELS: [LEVEL_COUNT]extension.Level = [LEVEL_COUNT]extension.Level;
```

## val RAX

```mach
pub val RAX: i32 = 0
```

## val RCX

```mach
pub val RCX: i32 = 1
```

## val RDX

```mach
pub val RDX: i32 = 2
```

## val RBX

```mach
pub val RBX: i32 = 3
```

## val RSP

```mach
pub val RSP: i32 = 4
```

## val RBP

```mach
pub val RBP: i32 = 5
```

## val RSI

```mach
pub val RSI: i32 = 6
```

## val RDI

```mach
pub val RDI: i32 = 7
```

## val R8

```mach
pub val R8:  i32 = 8
```

## val R9

```mach
pub val R9:  i32 = 9
```

## val R10

```mach
pub val R10: i32 = 10
```

## val R11

```mach
pub val R11: i32 = 11
```

## val R12

```mach
pub val R12: i32 = 12
```

## val R13

```mach
pub val R13: i32 = 13
```

## val R14

```mach
pub val R14: i32 = 14
```

## val R15

```mach
pub val R15: i32 = 15
```

## val XMM0

```mach
pub val XMM0:  i32 = 0
```

## val XMM1

```mach
pub val XMM1:  i32 = 1
```

## val XMM2

```mach
pub val XMM2:  i32 = 2
```

## val XMM3

```mach
pub val XMM3:  i32 = 3
```

## val XMM4

```mach
pub val XMM4:  i32 = 4
```

## val XMM5

```mach
pub val XMM5:  i32 = 5
```

## val XMM6

```mach
pub val XMM6:  i32 = 6
```

## val XMM7

```mach
pub val XMM7:  i32 = 7
```

## val XMM8

```mach
pub val XMM8:  i32 = 8
```

## val XMM9

```mach
pub val XMM9:  i32 = 9
```

## val XMM10

```mach
pub val XMM10: i32 = 10
```

## val XMM11

```mach
pub val XMM11: i32 = 11
```

## val XMM12

```mach
pub val XMM12: i32 = 12
```

## val XMM13

```mach
pub val XMM13: i32 = 13
```

## val XMM14

```mach
pub val XMM14: i32 = 14
```

## val XMM15

```mach
pub val XMM15: i32 = 15
```

## val GP_COUNT

```mach
pub val GP_COUNT: i32 = 16
```

## val FP_COUNT

```mach
pub val FP_COUNT: i32 = 16
```

## val STACK_REG

```mach
pub val STACK_REG: i32 = RSP
```

## val FRAME_REG

```mach
pub val FRAME_REG: i32 = RBP
```

## val SCRATCH_REG

```mach
pub val SCRATCH_REG:  i32 = R11
```

## val SCRATCH_REG2

```mach
pub val SCRATCH_REG2: i32 = R10
```

## def Opcode

```mach
pub def Opcode: u16
```

## val NOP

```mach
pub val NOP:       Opcode = 0
```

## val MOV

```mach
pub val MOV:       Opcode = 1
```

## val LEA

```mach
pub val LEA:       Opcode = 2
```

## val PUSH

```mach
pub val PUSH:      Opcode = 3
```

## val POP

```mach
pub val POP:       Opcode = 4
```

## val ADD

```mach
pub val ADD:       Opcode = 5
```

## val SUB

```mach
pub val SUB:       Opcode = 6
```

## val IMUL

```mach
pub val IMUL:      Opcode = 7
```

## val IDIV

```mach
pub val IDIV:      Opcode = 8
```

## val DIV

```mach
pub val DIV:       Opcode = 9
```

## val AND

```mach
pub val AND:       Opcode = 10
```

## val OR

```mach
pub val OR:        Opcode = 11
```

## val XOR

```mach
pub val XOR:       Opcode = 12
```

## val SHL

```mach
pub val SHL:       Opcode = 13
```

## val SHR

```mach
pub val SHR:       Opcode = 14
```

## val SAR

```mach
pub val SAR:       Opcode = 15
```

## val CMP

```mach
pub val CMP:       Opcode = 16
```

## val TEST

```mach
pub val TEST:      Opcode = 17
```

## val JMP

```mach
pub val JMP:       Opcode = 18
```

## val CALL

```mach
pub val CALL:      Opcode = 19
```

## val RET

```mach
pub val RET:       Opcode = 20
```

## val JE

```mach
pub val JE:        Opcode = 21
```

## val JNE

```mach
pub val JNE:       Opcode = 22
```

## val JL

```mach
pub val JL:        Opcode = 23
```

## val JLE

```mach
pub val JLE:       Opcode = 24
```

## val JG

```mach
pub val JG:        Opcode = 25
```

## val JGE

```mach
pub val JGE:       Opcode = 26
```

## val JB

```mach
pub val JB:        Opcode = 27
```

## val JBE

```mach
pub val JBE:       Opcode = 28
```

## val JA

```mach
pub val JA:        Opcode = 29
```

## val JAE

```mach
pub val JAE:       Opcode = 30
```

## val SETE

```mach
pub val SETE:      Opcode = 31
```

## val SETNE

```mach
pub val SETNE:     Opcode = 32
```

## val SETL

```mach
pub val SETL:      Opcode = 33
```

## val SETLE

```mach
pub val SETLE:     Opcode = 34
```

## val SETG

```mach
pub val SETG:      Opcode = 35
```

## val SETGE

```mach
pub val SETGE:     Opcode = 36
```

## val SETB

```mach
pub val SETB:      Opcode = 37
```

## val SETBE

```mach
pub val SETBE:     Opcode = 38
```

## val SETA

```mach
pub val SETA:      Opcode = 39
```

## val SETAE

```mach
pub val SETAE:     Opcode = 40
```

## val MOVSS

```mach
pub val MOVSS:     Opcode = 41
```

## val MOVSD

```mach
pub val MOVSD:     Opcode = 42
```

## val ADDSS

```mach
pub val ADDSS:     Opcode = 43
```

## val ADDSD

```mach
pub val ADDSD:     Opcode = 44
```

## val SUBSS

```mach
pub val SUBSS:     Opcode = 45
```

## val SUBSD

```mach
pub val SUBSD:     Opcode = 46
```

## val MULSS

```mach
pub val MULSS:     Opcode = 47
```

## val MULSD

```mach
pub val MULSD:     Opcode = 48
```

## val DIVSS

```mach
pub val DIVSS:     Opcode = 49
```

## val DIVSD

```mach
pub val DIVSD:     Opcode = 50
```

## val SYSCALL

```mach
pub val SYSCALL:   Opcode = 51
```

## val MOVABS

```mach
pub val MOVABS:    Opcode = 52
```

## val UD2

```mach
pub val UD2:       Opcode = 53
```

## val PAUSE

```mach
pub val PAUSE:     Opcode = 54
```

## val CDQ

```mach
pub val CDQ:       Opcode = 55
```

## val CQO

```mach
pub val CQO:       Opcode = 56
```

## val MOVSX

```mach
pub val MOVSX:     Opcode = 57
```

## val MOVZX

```mach
pub val MOVZX:     Opcode = 58
```

## val MOVSXD

```mach
pub val MOVSXD:    Opcode = 59
```

## val CVTSI2SS

```mach
pub val CVTSI2SS:  Opcode = 60
```

## val CVTSI2SD

```mach
pub val CVTSI2SD:  Opcode = 61
```

## val CVTSS2SD

```mach
pub val CVTSS2SD:  Opcode = 62
```

## val CVTSD2SS

```mach
pub val CVTSD2SS:  Opcode = 63
```

## val CVTTSS2SI

```mach
pub val CVTTSS2SI: Opcode = 64
```

## val CVTTSD2SI

```mach
pub val CVTTSD2SI: Opcode = 65
```

## val UCOMISS

```mach
pub val UCOMISS:   Opcode = 66
```

## val UCOMISD

```mach
pub val UCOMISD:   Opcode = 67
```

## val MOVSB

```mach
pub val MOVSB:     Opcode = 68
```

## val MFENCE

```mach
pub val MFENCE:    Opcode = 69
```

## val ASM_BLOCK

```mach
pub val ASM_BLOCK: Opcode = 70
```

## val CBW

```mach
pub val CBW:       Opcode = 71
```

## val CWD

```mach
pub val CWD:       Opcode = 72
```

## val MOVQ

```mach
pub val MOVQ:      Opcode = 73
```

## val XORPS

```mach
pub val XORPS:     Opcode = 74
```

## val NEG

```mach
pub val NEG:       Opcode = 75
```

## val NOT

```mach
pub val NOT:       Opcode = 76
```

## val INC

```mach
pub val INC:       Opcode = 77
```

## val DEC

```mach
pub val DEC:       Opcode = 78
```

## val HLT

```mach
pub val HLT:       Opcode = 79
```

## val XCHG

```mach
pub val XCHG:      Opcode = 80
```

## val XADD

```mach
pub val XADD:      Opcode = 81
```

## val CMPXCHG

```mach
pub val CMPXCHG:   Opcode = 82
```

## val IN

```mach
pub val IN:    Opcode = 120
```

## val OUT

```mach
pub val OUT:   Opcode = 121
```

## val CLI

```mach
pub val CLI:   Opcode = 122
```

## val STI

```mach
pub val STI:   Opcode = 123
```

## val LIDT

```mach
pub val LIDT:  Opcode = 124
```

## val RDTSC

```mach
pub val RDTSC: Opcode = 125
```

## val RDMSR

```mach
pub val RDMSR: Opcode = 126
```

## val WRMSR

```mach
pub val WRMSR: Opcode = 127
```

## val CLD

```mach
pub val CLD:     Opcode = 128
```

## val PUSHFQ

```mach
pub val PUSHFQ:  Opcode = 129
```

## val POPFQ

```mach
pub val POPFQ:   Opcode = 130
```

## val IRETQ

```mach
pub val IRETQ:   Opcode = 131
```

## val SWAPGS

```mach
pub val SWAPGS:  Opcode = 132
```

## val MOV_CR

```mach
pub val MOV_CR:  Opcode = 133
```

## val MOV_SEG

```mach
pub val MOV_SEG: Opcode = 134
```

## val PUNPCKLBW

```mach
pub val PUNPCKLBW: Opcode = 135
```

the packed sse2 lane-width and lane-kind conversions

## val PUNPCKLWD

```mach
pub val PUNPCKLWD: Opcode = 136
```

## val PUNPCKLDQ

```mach
pub val PUNPCKLDQ: Opcode = 137
```

## val PACKSSWB

```mach
pub val PACKSSWB:  Opcode = 138
```

## val PACKSSDW

```mach
pub val PACKSSDW:  Opcode = 139
```

## val PSLLW

```mach
pub val PSLLW:     Opcode = 140
```

## val PSRAW

```mach
pub val PSRAW:     Opcode = 141
```

## val PSRAD

```mach
pub val PSRAD:     Opcode = 142
```

## val CVTDQ2PS

```mach
pub val CVTDQ2PS:  Opcode = 143
```

## val CVTTPS2DQ

```mach
pub val CVTTPS2DQ: Opcode = 144
```

## val CVTDQ2PD

```mach
pub val CVTDQ2PD:  Opcode = 145
```

## val CVTTPD2DQ

```mach
pub val CVTTPD2DQ: Opcode = 146
```

## val CVTPS2PD

```mach
pub val CVTPS2PD:  Opcode = 147
```

## val CVTPD2PS

```mach
pub val CVTPD2PS:  Opcode = 148
```

## val MOVDQA

```mach
pub val MOVDQA: Opcode = 149
```

the integer-typed packed moves, reached only from inline asm

## val MOVDQU

```mach
pub val MOVDQU: Opcode = 150
```

## val RDFSBASE

```mach
pub val RDFSBASE: Opcode = 151
```

the fs and gs base register reads and writes, on a 32- or 64-bit register

## val RDGSBASE

```mach
pub val RDGSBASE: Opcode = 152
```

## val WRFSBASE

```mach
pub val WRFSBASE: Opcode = 153
```

## val WRGSBASE

```mach
pub val WRGSBASE: Opcode = 154
```

## val PMULHW

```mach
pub val PMULHW:  Opcode = 155
```

the high halves and the full unsigned product the packed widening multiplies read

## val PMULHUW

```mach
pub val PMULHUW: Opcode = 156
```

## val PMULUDQ

```mach
pub val PMULUDQ: Opcode = 157
```

## val SHA256RNDS2

```mach
pub val SHA256RNDS2: Opcode = 158
```

the extension rows, reached only from inline asm: sha, ssse3 and sse41, and
the baseline cpuid that detects them

## val SHA256MSG1

```mach
pub val SHA256MSG1:  Opcode = 159
```

## val SHA256MSG2

```mach
pub val SHA256MSG2:  Opcode = 160
```

## val PSHUFB

```mach
pub val PSHUFB:      Opcode = 161
```

## val PALIGNR

```mach
pub val PALIGNR:     Opcode = 162
```

## val PBLENDW

```mach
pub val PBLENDW:     Opcode = 163
```

## val PINSRD

```mach
pub val PINSRD:      Opcode = 164
```

## val PEXTRD

```mach
pub val PEXTRD:      Opcode = 165
```

## val PTEST

```mach
pub val PTEST:       Opcode = 166
```

## val CPUID

```mach
pub val CPUID:       Opcode = 167
```

## val MUL

```mach
pub val MUL: Opcode = 168
```

the one-operand unsigned widening multiply: r/m into rdx:rax

## val BSF

```mach
pub val BSF:    Opcode = 169
```

the bit-scan family, reached only from inline asm: the baseline scans and
byte swap, and the counts under popcnt, lzcnt and bmi1

## val BSR

```mach
pub val BSR:    Opcode = 170
```

## val POPCNT

```mach
pub val POPCNT: Opcode = 171
```

## val LZCNT

```mach
pub val LZCNT:  Opcode = 172
```

## val TZCNT

```mach
pub val TZCNT:  Opcode = 173
```

## val BSWAP

```mach
pub val BSWAP:  Opcode = 174
```

## val SHLD

```mach
pub val SHLD: Opcode = 175
```

the double-precision shifts: r/m shifted with bits fed in from a register

## val SHRD

```mach
pub val SHRD: Opcode = 176
```

## val PMULLD

```mach
pub val PMULLD: Opcode = 177
```

the sse41 packed lowering row: the 32-bit lane multiply the packed table
gates on the extension, reached from lowering and from inline asm alike

## val PUNPCKHBW

```mach
pub val PUNPCKHBW: Opcode = 178
```

the lane-halving extensions (#3738): the baseline high unpacks and the
byte shift that reach the high half, and the sse41 sign and zero extends
the encoder chooses on the same declared cells

## val PUNPCKHWD

```mach
pub val PUNPCKHWD: Opcode = 179
```

## val PUNPCKHDQ

```mach
pub val PUNPCKHDQ: Opcode = 180
```

## val PSRLDQ

```mach
pub val PSRLDQ:    Opcode = 181
```

## val PMOVSXBW

```mach
pub val PMOVSXBW:  Opcode = 182
```

## val PMOVSXWD

```mach
pub val PMOVSXWD:  Opcode = 183
```

## val PMOVSXDQ

```mach
pub val PMOVSXDQ:  Opcode = 184
```

## val PMOVZXBW

```mach
pub val PMOVZXBW:  Opcode = 185
```

## val PMOVZXWD

```mach
pub val PMOVZXWD:  Opcode = 186
```

## val PMOVZXDQ

```mach
pub val PMOVZXDQ:  Opcode = 187
```

## val PSRLQ

```mach
pub val PSRLQ:     Opcode = 188
```

## val PSLLQ

```mach
pub val PSLLQ:     Opcode = 189
```

## val PSRLW

```mach
pub val PSRLW:     Opcode = 190
```

## val PSRLD

```mach
pub val PSRLD:     Opcode = 191
```

## val SBB

```mach
pub val SBB: Opcode = 192
```

subtract with borrow: `sbb r, r` spreads the carry into every bit, the mask
a compare leaves (#3885)

## val RAW_BYTE

```mach
pub val RAW_BYTE: Opcode = 83
```

## val MOVAPS

```mach
pub val MOVAPS:   Opcode = 84
```

## val SETP

```mach
pub val SETP:     Opcode = 85
```

## val SETNP

```mach
pub val SETNP:    Opcode = 86
```

## val ADDPS

```mach
pub val ADDPS:   Opcode = 87
```

## val SUBPS

```mach
pub val SUBPS:   Opcode = 88
```

## val MULPS

```mach
pub val MULPS:   Opcode = 89
```

## val DIVPS

```mach
pub val DIVPS:   Opcode = 90
```

## val ADDPD

```mach
pub val ADDPD:   Opcode = 91
```

## val SUBPD

```mach
pub val SUBPD:   Opcode = 92
```

## val MULPD

```mach
pub val MULPD:   Opcode = 93
```

## val DIVPD

```mach
pub val DIVPD:   Opcode = 94
```

## val PADDB

```mach
pub val PADDB:   Opcode = 95
```

## val PADDW

```mach
pub val PADDW:   Opcode = 96
```

## val PADDD

```mach
pub val PADDD:   Opcode = 97
```

## val PADDQ

```mach
pub val PADDQ:   Opcode = 98
```

## val PSUBB

```mach
pub val PSUBB:   Opcode = 99
```

## val PSUBW

```mach
pub val PSUBW:   Opcode = 100
```

## val PSUBD

```mach
pub val PSUBD:   Opcode = 101
```

## val PSUBQ

```mach
pub val PSUBQ:   Opcode = 102
```

## val PMULLW

```mach
pub val PMULLW:  Opcode = 103
```

## val PAND

```mach
pub val PAND:    Opcode = 104
```

## val POR

```mach
pub val POR:     Opcode = 105
```

## val PXOR

```mach
pub val PXOR:    Opcode = 106
```

## val CMPPS

```mach
pub val CMPPS:   Opcode = 107
```

## val CMPPD

```mach
pub val CMPPD:   Opcode = 108
```

## val PCMPEQB

```mach
pub val PCMPEQB: Opcode = 109
```

## val PCMPEQW

```mach
pub val PCMPEQW: Opcode = 110
```

## val PCMPEQD

```mach
pub val PCMPEQD: Opcode = 111
```

## val PCMPGTB

```mach
pub val PCMPGTB: Opcode = 112
```

## val PCMPGTW

```mach
pub val PCMPGTW: Opcode = 113
```

## val PCMPGTD

```mach
pub val PCMPGTD: Opcode = 114
```

## val MOVUPS

```mach
pub val MOVUPS:  Opcode = 115
```

## val PSHUFD

```mach
pub val PSHUFD:  Opcode = 116
```

## val PSLLD

```mach
pub val PSLLD:   Opcode = 117
```

## val PSUBUSB

```mach
pub val PSUBUSB: Opcode = 118
```

## val PSUBUSW

```mach
pub val PSUBUSW: Opcode = 119
```

## def EncFlag

```mach
pub def EncFlag: u16
```

## val FLAG_REX_W

```mach
pub val FLAG_REX_W:    EncFlag = 1
```

## val FLAG_16BIT

```mach
pub val FLAG_16BIT:    EncFlag = 2
```

## val FLAG_LOCK

```mach
pub val FLAG_LOCK:     EncFlag = 4
```

## val FLAG_REP

```mach
pub val FLAG_REP:      EncFlag = 8
```

## val FLAG_INDIRECT

```mach
pub val FLAG_INDIRECT: EncFlag = 16
```

## val FLAG_LOCAL_FWD

```mach
pub val FLAG_LOCAL_FWD: EncFlag = 32
```

an inline-asm transfer to a numbered local label that lies ahead

## val MEM_BASE_ABS

```mach
pub val MEM_BASE_ABS: i32 = -2
```

## def Segment

```mach
pub def Segment: u8
```

the segment override a memory operand carries (isa.Operand.seg). only fs
and gs relocate an address in long mode; es, cs, ss and ds are ignored

## val SEG_NONE

```mach
pub val SEG_NONE: Segment = 0
```

## val SEG_FS

```mach
pub val SEG_FS:   Segment = 1
```

## val SEG_GS

```mach
pub val SEG_GS:   Segment = 2
```

## rec SegmentRow

```mach
pub rec SegmentRow;
```

## val SEGMENT_COUNT

```mach
pub val SEGMENT_COUNT: usize = 2
```

## val SEGMENTS

```mach
pub val SEGMENTS: [SEGMENT_COUNT]SegmentRow = [SEGMENT_COUNT]SegmentRow;
```

## fun segment_row

```mach
pub fun segment_row(seg: u8) *SegmentRow;
```

the row of a segment override; nil for SEG_NONE and anything unassigned

## def Form

```mach
pub def Form: u8
```

the instruction form of an opcode: the operand shape the encoder admits and
the encoder family that emits it. every opcode the stream can carry has
exactly one; FORM_NONE is the absence of a row and every reader refuses it

## val FORM_NONE

```mach
pub val FORM_NONE: Form = 0
```

## val FORM_BARE

```mach
pub val FORM_BARE: Form = 1
```

fixed bytes, no explicit operand

## val FORM_MOV

```mach
pub val FORM_MOV: Form = 2
```

mov: dst r/m, src r/m, imm or sym

## val FORM_LEA

```mach
pub val FORM_LEA: Form = 3
```

lea: dst r, src m taken as an address

## val FORM_PUSH

```mach
pub val FORM_PUSH: Form = 4
```

push: src r or imm, through the stack

## val FORM_POP

```mach
pub val FORM_POP: Form = 5
```

pop: dst r, through the stack

## val FORM_ALU

```mach
pub val FORM_ALU: Form = 6
```

add sub and or xor cmp: dst r/m, src r/m or imm

## val FORM_TEST

```mach
pub val FORM_TEST: Form = 7
```

test: dst r/m, src r or imm

## val FORM_IMUL

```mach
pub val FORM_IMUL: Form = 8
```

imul: src r/m into rdx:rax, dst r with src r/m, or dst r with src r/m and imm

## val FORM_DIV

```mach
pub val FORM_DIV: Form = 9
```

idiv div: src r/m over rdx:rax

## val FORM_SHIFT

```mach
pub val FORM_SHIFT: Form = 10
```

shl shr sar: dst r/m, count imm or cl

## val FORM_JCC

```mach
pub val FORM_JCC: Form = 11
```

jcc: a label

## val FORM_JMP

```mach
pub val FORM_JMP: Form = 12
```

jmp: a label, a sym, or r/m

## val FORM_CALL

```mach
pub val FORM_CALL: Form = 13
```

call: a sym or r/m, through the stack

## val FORM_RET

```mach
pub val FORM_RET: Form = 14
```

ret iretq: through the stack

## val FORM_SETCC

```mach
pub val FORM_SETCC: Form = 15
```

setcc: dst r8

## val FORM_MOVX

```mach
pub val FORM_MOVX: Form = 16
```

movzx movsx movsxd: dst r, src r/m

## val FORM_MOVABS

```mach
pub val FORM_MOVABS: Form = 17
```

movabs: dst r, imm64 or sym

## val FORM_MOVQ

```mach
pub val FORM_MOVQ: Form = 18
```

movq: r to xmm or xmm to r

## val FORM_SSE_MOV

```mach
pub val FORM_SSE_MOV: Form = 19
```

movss movsd: xmm to xmm or m, m to xmm

## val FORM_SSE_MOVA

```mach
pub val FORM_SSE_MOVA: Form = 20
```

movaps movups: xmm to xmm or m128, m128 to xmm

## val FORM_SSE_ALU

```mach
pub val FORM_SSE_ALU: Form = 21
```

scalar sse arithmetic: dst xmm, src xmm or m

## val FORM_UCOMIS

```mach
pub val FORM_UCOMIS: Form = 22
```

ucomiss ucomisd: xmm, xmm or m

## val FORM_CVT

```mach
pub val FORM_CVT: Form = 23
```

scalar conversions between r and xmm

## val FORM_XORPS

```mach
pub val FORM_XORPS: Form = 24
```

xorps: dst xmm, src xmm or m128

## val FORM_PACKED

```mach
pub val FORM_PACKED: Form = 25
```

packed sse2 arithmetic, logic and compare: dst xmm, src xmm or m128

## val FORM_PSHUF

```mach
pub val FORM_PSHUF: Form = 26
```

pshufd: dst xmm, src xmm, imm8

## val FORM_PSLL

```mach
pub val FORM_PSLL: Form = 27
```

pslld psllw psraw psrad: dst xmm, imm8

## val FORM_GRP3

```mach
pub val FORM_GRP3: Form = 28
```

neg not: r/m

## val FORM_GRP5

```mach
pub val FORM_GRP5: Form = 29
```

inc dec: r/m

## val FORM_XCHG

```mach
pub val FORM_XCHG: Form = 30
```

xchg: r, r/m

## val FORM_XADD

```mach
pub val FORM_XADD: Form = 31
```

xadd cmpxchg: r/m, r

## val FORM_PORTIO

```mach
pub val FORM_PORTIO: Form = 32
```

in out: al ax eax with dx or imm8

## val FORM_LIDT

```mach
pub val FORM_LIDT: Form = 33
```

lidt: m

## val FORM_MOV_CR

```mach
pub val FORM_MOV_CR: Form = 34
```

mov to or from a control register

## val FORM_MOV_SEG

```mach
pub val FORM_MOV_SEG: Form = 35
```

mov to or from a segment register

## val FORM_STRING

```mach
pub val FORM_STRING: Form = 36
```

movsb: rsi to rdi, rcx under rep

## val FORM_TRAP

```mach
pub val FORM_TRAP: Form = 37
```

ud2 hlt: never continues

## val FORM_DATA

```mach
pub val FORM_DATA: Form = 38
```

a data byte in the stream (raw_byte), not an instruction

## val FORM_FSGSBASE

```mach
pub val FORM_FSGSBASE: Form = 39
```

rdfsbase rdgsbase wrfsbase wrgsbase: r32 or r64

## val FORM_PINSR

```mach
pub val FORM_PINSR: Form = 40
```

pinsrd pextrd: an xmm and an r32 or m32, with imm8

## val FORM_MUL1

```mach
pub val FORM_MUL1: Form = 41
```

mul: src r/m by rax into rdx:rax

## val FORM_BITSCAN

```mach
pub val FORM_BITSCAN: Form = 42
```

bsf bsr popcnt lzcnt tzcnt: a 16-, 32- or 64-bit register from an r/m of the same width

## val FORM_BSWAP

```mach
pub val FORM_BSWAP: Form = 43
```

bswap: one 32- or 64-bit register, reversed in place

## val FORM_DSHIFT

```mach
pub val FORM_DSHIFT: Form = 44
```

shld shrd: an r/m, the register feeding bits, and a count of imm8 or cl

## val FORM_CARRY

```mach
pub val FORM_CARRY: Form = 45
```

sbb: r/m less a register or imm and the carry, which it reads

## val FORM_COUNT

```mach
pub val FORM_COUNT: u32 = 46
```

## def Shape

```mach
pub def Shape: u8
```

the role pattern of the explicit operands, in intel order

## val SHAPE_NONE

```mach
pub val SHAPE_NONE: Shape = 0
```

## val SHAPE_DEF

```mach
pub val SHAPE_DEF: Shape = 1
```

the first operand is written, the second read

## val SHAPE_RW

```mach
pub val SHAPE_RW: Shape = 2
```

the first operand is read and written, the second read

## val SHAPE_USE

```mach
pub val SHAPE_USE: Shape = 3
```

every operand is read

## val SHAPE_SWAP

```mach
pub val SHAPE_SWAP: Shape = 4
```

every operand is read and written

## val SHAPE_XFER

```mach
pub val SHAPE_XFER: Shape = 5
```

the operand is a transfer target

## val SHAPE_COUNT

```mach
pub val SHAPE_COUNT: u32 = 6
```

## def MemEffect

```mach
pub def MemEffect: u8
```

how the instruction reaches memory, as bits

## val MEM_NONE

```mach
pub val MEM_NONE: MemEffect = 0
```

## val MEM_OPERAND

```mach
pub val MEM_OPERAND: MemEffect = 1
```

through an r/m operand, read or written as that position's role says

## val MEM_ADDR

```mach
pub val MEM_ADDR: MemEffect = 2
```

a memory operand read as an address only, never accessed

## val MEM_STACK

```mach
pub val MEM_STACK: MemEffect = 4
```

through rsp with no explicit address

## val MEM_STRING

```mach
pub val MEM_STRING: MemEffect = 8
```

through rsi and rdi with no explicit address

## val MEM_BASE

```mach
pub val MEM_BASE: MemEffect = 16
```

the register operand becomes a segment base that later fs- or gs-relative
operands address through, so its value is an address

## val MEM_ALL

```mach
pub val MEM_ALL: MemEffect = 31
```

## rec OpDesc

```mach
pub rec OpDesc;
```

the description of one opcode: its form and operand shape, the flags it
reads and writes with its latency class, and how it reaches memory. the
encoder, the effect walk, the inline-asm scan and the range predicates
all read this row and nothing else describes an opcode

## fun describe

```mach
pub fun describe(op: u16) OpDesc;
```

the row of an opcode; the two pseudo entries and anything outside the
catalog describe as FORM_NONE with an unknown class

## fun form_is_float

```mach
pub fun form_is_float(form: Form) bool;
```

the forms whose operands live in the xmm file, wholly or on one side

## fun is_fp_opcode

```mach
pub fun is_fp_opcode(op: u16) bool;
```

## fun reg_name

```mach
pub fun reg_name(ctx: ptr, id: i32, size: u8) str;
```

## fun fp_reg_name

```mach
pub fun fp_reg_name(cid: i32) str;
```

## val CALLEE_SAVED_GP_MAX

```mach
pub val CALLEE_SAVED_GP_MAX: i32 = GP_COUNT
```

## fun callee_saved_gp_list

```mach
pub fun callee_saved_gp_list(abi_vt: *abi.AbiVTable, out: *i32) i32;
```

## fun callee_saved_gp_list_from

```mach
pub fun callee_saved_gp_list_from(fill: isa.RegFileFn, out: *i32) i32;
```

