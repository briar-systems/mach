# mach.lang.target.of.coff.imports

## rec Expanded

```mach
pub rec Expanded;
```

## fun dnit

```mach
pub fun dnit(out: *Expanded);
```

## fun expand

```mach
pub fun expand(input: *of.ObjectImage, out: *Expanded) err[fail.Fail];
```

## fun normalize

```mach
pub fun normalize(img: *of.ObjectImage) err[fail.Fail];
```

