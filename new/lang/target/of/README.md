# lang/target/of

Object format implementations. Each conforms to the interface defined in `../of.mach`.

## Files

- `elf.mach` — Executable and Linkable Format (Linux, BSD).
- `macho.mach` — Mach-O (macOS, iOS).
- `coff.mach` — Common Object File Format / PE (Windows).
