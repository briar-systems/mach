# lang/target/abi

ABI implementations. Each conforms to the interface defined in `../abi.mach`.

## Files

- `sysv.mach` — System V AMD64 calling convention (Linux, BSD, macOS).
- `win64.mach` — Microsoft x64 calling convention (Windows).
