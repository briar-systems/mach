# lang/target/os

Operating system implementations. Each conforms to the interface defined in `../os.mach`.

## Files

- `linux.mach` — Linux: entry point, syscall ABI, default library layout.
- `darwin.mach` — macOS / Darwin.
- `windows.mach` — Microsoft Windows.
